import LabelData from "../../../Components/LabelData";
import { labelJobData } from "../Data/LabelJobdata";

const LabelJobData = () => {
  return <LabelData labels={labelJobData} />;
};

export default LabelJobData;
